{
  "name": "ai-bloodloss-monitor",
  "version": "1.0.0",
  "description": "AI Blood Loss Estimator & Fluid Monitor - Full Stack",
  "scripts": {
    "dev": "node start.js",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
